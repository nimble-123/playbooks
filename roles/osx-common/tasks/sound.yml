---
# Sound

- name: Disable the sound effects on boot
  command: nvram SystemAudioVolume=" "
  sudo: yes
  tags: sudo

- name: Disable user interface sound effects
  raw: defaults write com.apple.systemsound com.apple.sound.uiaudio.enabled -int 0
